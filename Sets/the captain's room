#solution i used did not use sets
k = int(input())
d = {}
lst = list(map(int,input().split()))
for num in lst:
    d[num] = d.get(num,0)+1
for key in d:
    if d[key] == 1:
        print(key)
        break
